source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://gitlab.com/#{repo_name}" }
# Specify your gem's dependencies in mikef_palindrome.gemspec
gemspec

gem 'minitest-reporters', '1.2.0'
